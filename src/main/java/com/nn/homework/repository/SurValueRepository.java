package com.nn.homework.repository;

import com.nn.homework.domain.SurValue;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SurValueRepository extends JpaRepository<SurValue, Long> {
}